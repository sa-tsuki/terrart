import React, { memo } from 'react'

const about = () => {
    return (
        <section id="about">
            <div className="max-width">
                About page
            </div>
        </section>
    )
}
export default memo(about)
