import React, { memo } from 'react'

const contact = () => {
    return (
        <section id="contact">
            <div className="max-width">
                Contact page
            </div>
        </section>
    )
}
export default memo(contact)
